<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.test.mapper.SkuMapper">
    <select id="selectListBySpuId" resultType="com.test.entity.Sku">
        select * from sku where sku_spu_id = #{spuId}
    </select>
</mapper>